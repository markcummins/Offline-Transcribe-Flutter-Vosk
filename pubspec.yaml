name: vosk
description: A new Flutter project.
publish_to: "none"
version: 0.1.0

environment:
  sdk: ">=3.0.6 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  record: ^5.0.5

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/models/vosk-model-spk-0.4.zip
    - assets/models/vosk-model-small-en-us-0.15.zip
    - assets/models/vosk-model-en-us-0.22-lgraph.zip
